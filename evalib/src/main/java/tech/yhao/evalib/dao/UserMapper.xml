<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="tech.yhao.evalib.dao.UserMapper">
	<sql id="userColumns">
		id, name,
		description, created_at as
		createdAt,
		updated_at as
		updatedAt
	</sql>

	<select id="findById" parameterType="uuid" resultType="User">
		select
		<include refid="userColumns"></include>
		from t_user
		where id=#{id}
	</select>

	<select id="findByName" parameterType="string" resultType="User">
		select
		<include refid="userColumns"></include>
		from
		t_user
		where name=#{name}
	</select>
</mapper>